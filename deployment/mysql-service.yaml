apiVersion: v1
kind: Service
metadata:
  name: mysql-service-lb
  namespace: ats-space
spec:
  selector:
    app: mysql-app
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30082
  type: LoadBalancer